{
  "name": "toolpay",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev:web": "pnpm --filter httpay-website dev",
    "build:web": "pnpm --filter httpay-website build"
  }
}
